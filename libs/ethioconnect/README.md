# ethioconnect

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ethioconnect` to build the library.

## Running unit tests

Run `nx test ethioconnect` to execute the unit tests via [Jest](https://jestjs.io).
